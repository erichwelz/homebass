<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Musician Finder!" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
    <body>
      <nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
            <h1><a href="/">MusicianFinder</a></h1>
          </li>
        </ul>

        <section class="top-bar-section">
          <ul class="right">
            <% if current_user %>
              <li><%= link_to "Logout", :logout, method: :post %></li>
            <% else %>
              <li><%= link_to "Register", new_user_path %></li> 
              <li><%= link_to "Login", :login %></li>
            <% end %>
          </ul>
          
          <ul class="left">
              <% if current_user %>
                <li>
                  <%= link_to "My Profile(#{current_user.invitations_received.where(read: false).count})", user_path(current_user.id) %>          
                </li>  
                <li>
                  <%= link_to "Edit Profile", edit_user_path(current_user.id) %></a>
                </li>
              <% end %>
          </ul>
        </section>
      </nav>

<div>
  <p id="notice"><%= flash[:notice] %></p>
  <p id="alert"><%= flash[:alert] %></p>
</div>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>